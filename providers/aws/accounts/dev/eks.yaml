# The contents of this file is merged with other files. Run `sugarkube cluster vars` (see `-h` for valid parameters) to
# see how this gets merged together.
provisioner:
  params:
    config_file:
      nodeGroups:
        - name: "{{ .node_group_name }}"
          instanceType: t3a.small
          desiredCapacity: 2
